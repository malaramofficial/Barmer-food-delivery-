import { createClient } from 'https://esm.sh/@supabase/supabase-js@2';
const cors={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, x-client-info, apikey, content-type'};
Deno.serve(async req=>{if(req.method==='OPTIONS')return new Response('ok',{headers:cors});const service=createClient(Deno.env.get('SUPABASE_URL')!,Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')!);const checks:any={database:false,orders:false};const {error}=await service.from('orders').select('id',{count:'exact',head:true});checks.database=!error;checks.orders=!error;return new Response(JSON.stringify({ok:!error,service:'barmer-food-delivery',timestamp:new Date().toISOString(),checks}),{status:error?503:200,headers:{...cors,'Content-Type':'application/json'}});});
